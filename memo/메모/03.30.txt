함수(function, method)
기능 - 입력, 추력, 사칙연산

function 함수이름(){
   실행문(변수,제어문(if,for))
}

sayHello("유관순")
입력 - prompt(), 함수의 매개변수(파라미터)

return이 없는 함수 - 매개변수 
return이 있는 함수 - 매개변수

배열(Array)
- 여러개의 연속적인 값을 저장하는 자료형
let 배열이름 = [값1, 값2...]

유형2
let 배열이름 = new Array(4)

getElementbyld()
- 글자를 클릭하면 글자색 바꾸기